using UnityEngine;

namespace Sudoku.UI.Infrastructure
{

/**
 * Marker component attached to UI objects that were created at runtime by
 * the runtime UI helpers. Used to locate and clean up generated UI.
 */
public class GeneratedRuntimeUI : MonoBehaviour
{
}
}
